using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class AdaptButtonSizeToScreen : MonoBehaviour {

	// Use this for initialization
	void Start () {
        /*
        GridLayoutGroup g = this.GetComponent<GridLayoutGroup>();

        float scale =  Screen.width / Screen.dpi / 3;

        float desiredX = 250;
        float desiredY = 180;

        float factor = desiredX / desiredY;

        int desiredSpacing = (int)g.spacing.x * 2;

        g.cellSize = new Vector2(scale * desiredX, scale * desiredY);
        g.spacing = new Vector2(scale * desiredSpacing, scale * desiredSpacing);
        */
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
